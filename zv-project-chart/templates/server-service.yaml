apiVersion: v1
kind: Service
metadata:
  name: zv-project-server
spec:
  type: NodePort
  selector:
    app: zv-project-server
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080